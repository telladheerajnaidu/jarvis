# Copy to .env.local and fill with your admin cred.
# .env.local is gitignored. Never commit real creds.

JARVIS_SECRET=change-me-in-prod

# Single admin login (set the same three in Vercel env)
JARVIS_ADMIN_EMAIL=admin@example.com
JARVIS_ADMIN_PASSWORD=changeme
JARVIS_ADMIN_NAME=Admin
